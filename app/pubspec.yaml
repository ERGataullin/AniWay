name: app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  auth:
    path: ../features/common/auth
  core: 
    path: ../core
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router: ^14.1.1
  hive: ^2.2.3
  l10n:
    path: ../features/shared/l10n
  movies: 
    path: ../features/common/movies
  path_provider: ^2.1.1
  player:
    path: ../features/common/player
  root_menu:
    path: ../features/common/root_menu

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
