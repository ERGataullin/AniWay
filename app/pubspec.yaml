name: app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  auth:
    path: ../modules/auth
  core: 
    path: ../core
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router: ^13.0.0
  movies: 
    path: ../modules/movies
  path_provider: ^2.1.1
  player:
    path: ../modules/player
  root_menu:
    path: ../modules/root_menu

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
